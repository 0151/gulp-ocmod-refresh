# Gulp plugin for updating OCMOD cache (OpenCart 3.x)
## Using
```javascript
const ocmodRefresh = require('gulp-ocmod-refresh')

ocmodRefresh({
    url: 'localhost',
    username: 'admin',
    password: 'secret',
})
```